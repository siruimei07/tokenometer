# Tokenometer

Windows desktop UI for monitoring Codex token usage. The current milestone uses clearly labeled demo data while the read-only Codex collector is developed.

```powershell
dotnet run --project .\src\Tokenometer.App\Tokenometer.App.csproj
```

Requires Windows 10/11 and .NET 10.
